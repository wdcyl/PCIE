# an9238_capture.sv

在65 MHz采样时钟域同时读取CH0和CH1。每路12 bit用16 bit容器保存，同一时刻两路组成32 bit；累计四组后写出128 bit。`start_i`和`clear_i`通过toggle跨时钟，采样长度在命令前保持稳定后同步。

采集长度必须是4个采样对的整数倍。ADC不能接受背压，因此在需要提交128-bit字时发现缓存满，模块会终止本次采集并置`overflow_o`，避免静默丢样。
